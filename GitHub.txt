Download Git
git --version
git config --global user.name "Shivam Kushwaha"
git config --global user.email "mrshivam.in@gmail.com"
git config --global user.name
git config --global user.email
git config --globel --edit
mkdir global file
cd global file
git init
git add global file
git status
git commit -m "initial commit"
git log (For who commmit)
git add . (all file in staging area)
git status
git checkout (commit id)
git branch
git checkout master
git branch
>git checkout dev
>>git checkout -b shivam/multiply
git status
>>git add global file2
>>git commit -m "add global file-2"
git checkout dev
git merge shivam/multiply
git merge dev
git log
3 cmd for push-after create repository

git add .
git commit - m "update file"
git push